---
title: cancel_crypto_order
---
import { DocParameterTable, DocDataFrame } from '../../../components/customComponents.js';

# cancel_crypto_order

Cancels a crypto order with the given order ID.

## Syntax

<DocParameterTable params={[
    {'field': 'order_id', 'type': 'str', 'description': 'ID of the crypto order'},
]} />

```python
def cancel_crypto_order(self, order_id) -> None:
```

## Returns

This method does not return anything.

## Raises
- A `NotImplementedError` is raised if the method is not supported.
---
