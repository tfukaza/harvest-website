---
title: get_current_time
---
import { DocParameterTable, DocDataFrame } from '../../../components/customComponents.js';

# get_current_time

Returns the current time in UTC timezone, accurate to the minute.

## Syntax

<DocParameterTable params={[]} />

```python
def get_current_time(self) -> dt.datetime:
```

## Returns

Returns the current time as a datetime object in UTC timezone.

## Raises
- An `Exception` is raised if there is an error retrieving the current time.
---
