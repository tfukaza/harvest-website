---
title: cancel_option_order
---
import { DocParameterTable, DocDataFrame } from '../../../components/customComponents.js';

# cancel_option_order

Cancels an option order with the given order ID.

## Syntax

<DocParameterTable params={[
    {'field': 'order_id', 'type': 'str', 'description': 'ID of the option order'},
]} />

```python
def cancel_option_order(self, order_id) -> None:
```

## Returns

This method does not return anything.

## Raises
- A `NotImplementedError` is raised if the method is not supported.
---
